#include<stdio.h>
#include<fcntl.h>
#include<unistd.h>
#include<sys/types.h>
#include<string.h>
#include<sys/wait.h>
#include<stdlib.h>
	char buf[]="I miss you\0";
	char buf1[]="I still miss you\0";
	int fd = open ("test",O_RDWR);
	int id;
	if((id=fork())==0){
		printf("I am in the child\n");
		if(lseek(fd,0,2)<0)printf("error lseek in child");
		if(write(fd,buf,strlen(buf))<0)printf("error write in child");		
		exit(0);
	}

	printf("I am in the parent\n");
	if(lseek(fd,0,2)<0)printf("error lseek in parent");
	if(write(fd,buf1,strlen(buf))<0)printf("error write in parent");	

	wait(NULL);
	exit(0);
}
/
/*
This is a test.
And I love you my little qiu.
I still miI miss you

The above is the result , we can reasily obtain that the result which is not
we want . and the output order is incorrect.

*/




































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































